!---
description: Собрать страницу по шаблону из вёрстки
---

Требуется собрать главную страницу по примеру верстки `source/index.3.html`, определи секции, блоки, разбей их по ui компонентам `shared` , `form`, подключи навык `seo-write`, для описания страницы под главный запрос `техпомощь тюмень` подготовь сайт под кластерный охват запросов и использованием `cluster-pillar-page`, (`uslugi` , `blog`, `landing`) используй документацию по этапам реализации проекта