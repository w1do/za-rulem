---
import { findCity } from '../../lib/cities';

export const prerender = false;

const city = findCity(Astro.params.city);
if (!city) return new Response('Город не найден', { status: 404 });

return Astro.redirect(`/terms${Astro.url.search}`, 301);
---
